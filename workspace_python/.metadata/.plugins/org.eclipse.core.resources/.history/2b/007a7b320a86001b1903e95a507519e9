import pymysql

conn = pymysql.connect(
    host = 'localhost',
    user = 'root',
    password = 'python',
    db='python',
    charset='utf8'
    )

try:
    cur = conn.cursor()
    cur.execute('select * from sample')
    print(cur.fetchone())