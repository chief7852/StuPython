# input("시작수를 넣으세요") 단 작은수
from comtypes.test.test_createwrappers import number
start = input("시작수를 넣으시오\n")
# input("끝수를 넣으세요")
end = input("끝수를 넣으세요\n")
# a 에서 b까지의 합을 구하세요
sum = 0;
if type(start + end) == number:
    for i in range(start,end+1):
         sum +=i